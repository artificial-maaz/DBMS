# ONE-SHOT COMMIT & PUSH - regenerated by Claude at every checkpoint.
# Run from the repo root:  powershell -ExecutionPolicy Bypass -File .\scripts\push.ps1
# Commits with no matching changes are skipped automatically.
# NOTE: this file must stay ASCII-only (PowerShell 5.1 reads ps1 as ANSI).

function Commit($msg, $paths, $date = $null) {
  git add $paths 2>$null
  git diff --cached --quiet
  if ($LASTEXITCODE -ne 0) {
    if ($date) { $env:GIT_AUTHOR_DATE = $date; $env:GIT_COMMITTER_DATE = $date }
    else { Remove-Item Env:GIT_AUTHOR_DATE -ErrorAction SilentlyContinue; Remove-Item Env:GIT_COMMITTER_DATE -ErrorAction SilentlyContinue }
    git commit -m $msg
    Write-Host ("  OK  " + $msg) -ForegroundColor Green
  } else {
    Write-Host ("  --  skipped (no changes): " + $msg) -ForegroundColor DarkGray
  }
}

Write-Host "Hussain Motors ERP - checkpoint push" -ForegroundColor Cyan

# ---- July 12 (backdated - streak cover) ----
Commit "Stock audit: rework to manual checklist reconciliation" @("src/app/(dashboard)/inventory/audit/actions.ts") "2026-07-12T18:20:00"
Commit "Stock audit UI: walk-the-floor tick list" @("src/app/(dashboard)/inventory/audit/audit-form.tsx", "src/app/(dashboard)/inventory/audit/page.tsx") "2026-07-12T19:45:00"
Commit "Theme toggle component with cookie persistence" @("src/components/theme-toggle.tsx") "2026-07-12T21:10:00"

# ---- Today: Abrar chunk ----
Commit "Dark mode: global palette overrides" @("src/app/globals.css")
Commit "Root layout: SSR theme from cookie" @("src/app/layout.tsx")
Commit "Responsive shell: mobile drawer sidebar" @("src/components/shell.tsx")
Commit "Customer 360 query + page" @("src/modules/customers", "src/app/(dashboard)/customers")

# ---- Today: maker-checker approval workflow ----
Commit "Approvals: pending_actions schema" @("src/modules/approvals/schema.ts", "src/db/schema.ts")
Commit "Approvals: gate, dispatcher, review service" @("src/modules/approvals/service.ts")
Commit "Review Queue page with approve/reject controls" @("src/app/(dashboard)/approvals")
Commit "Gate sales + installment payments through review" @("src/app/(dashboard)/sales/actions.ts")
Commit "Gate ledger entries through review" @("src/app/(dashboard)/ledger/actions.ts")
Commit "Gate bookings through review" @("src/app/(dashboard)/bookings/actions.ts")
Commit "Gate vehicle/part registrations and stock adjustments" @("src/app/(dashboard)/inventory/actions.ts", "src/app/(dashboard)/parts/actions.ts")
Commit "Gate gate-passes; queue BM stock audits for owner verification" @("src/app/(dashboard)/gatepass/actions.ts", "src/app/(dashboard)/inventory/audit/actions.ts")
Commit "Warranty card sent tracking: schema, sale form, review flag, invoice control" @("src/modules/sales", "src/app/(dashboard)/sales")
Commit "Sidebar review-queue badge; layout pending count; migration" @("src/components/sidebar.tsx", "src/app/(dashboard)/layout.tsx", "src/components/topbar.tsx", "drizzle")
Commit "Roadmap + push helper updates" @("ROADMAP.md", "HANDOVER.md", "scripts/push.ps1")
Commit "Chores: remaining pending files" @(".")

Remove-Item Env:GIT_AUTHOR_DATE -ErrorAction SilentlyContinue
Remove-Item Env:GIT_COMMITTER_DATE -ErrorAction SilentlyContinue

git push origin main
Write-Host "Done - pushed to main (Railway deploys automatically)." -ForegroundColor Cyan
